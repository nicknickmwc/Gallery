<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Тень -->
    <item android:bottom="224dp" android:left="224dp" android:right="224dp" android:top="224dp">
        <shape android:shape="rectangle">
            <solid android:color="#000000" /> <!-- Цвет тени -->
            <corners android:radius="8dp" /> <!-- Радиус углов -->
        </shape>
    </item>

    <!-- Основной фоновый цвет -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Основной цвет фона -->
            <corners android:radius="8dp" /> <!-- Радиус углов -->
        </shape>
    </item>
</layer-list>